config ZMK_LAYER_REPORT
    bool "Send active layer to host via Raw HID"
    depends on RAW_HID
    default y

if ZMK_LAYER_REPORT

config ZMK_LAYER_REPORT_DEBOUNCE_MS
    int "Debounce time for layer reports in ms"
    default 30

endif
